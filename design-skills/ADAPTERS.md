# Design Skill Adapter Backlog

Third-batch Open Design skills are preserved in `design-skills/` but remain hidden until an Aurora adapter makes them executable.

Total pending: 48

## audio (2)

- `audio-jingle` — runtime-toolchain; required tools: adapter not wired
- `speech` — external-service; required tools: adapter not wired

## document (8)

- `docx` — document-toolchain; required tools: adapter not wired
- `minimax-docx` — document-toolchain; required tools: adapter not wired
- `minimax-pdf` — document-toolchain; required tools: adapter not wired
- `pdf` — document-toolchain; required tools: adapter not wired
- `ppt-keynote` — document-toolchain; required tools: adapter not wired
- `pptx` — document-toolchain; required tools: adapter not wired
- `pptx-generator` — document-toolchain; required tools: adapter not wired
- `pptx-html-fidelity-audit` — document-toolchain; required tools: adapter not wired

## external (2)

- `full-page-screenshot` — external-service; required tools: adapter not wired
- `screenshot` — external-service; required tools: adapter not wired

## fal (12)

- `fal-3d` — external-service; required tools: adapter not wired
- `fal-generate` — external-service; required tools: adapter not wired
- `fal-image-edit` — external-service; required tools: adapter not wired
- `fal-kling-o3` — external-service; required tools: adapter not wired
- `fal-lip-sync` — external-service; required tools: adapter not wired
- `fal-realtime` — external-service; required tools: adapter not wired
- `fal-restore` — external-service; required tools: adapter not wired
- `fal-train` — external-service; required tools: adapter not wired
- `fal-tryon` — external-service; required tools: adapter not wired
- `fal-upscale` — external-service; required tools: adapter not wired
- `fal-video-edit` — external-service; required tools: adapter not wired
- `fal-vision` — external-service; required tools: adapter not wired

## figma (7)

- `figma-code-connect-components` — external-service; required tools: adapter not wired
- `figma-create-design-system-rules` — external-service; required tools: adapter not wired
- `figma-create-new-file` — external-service; required tools: adapter not wired
- `figma-generate-design` — external-service; required tools: adapter not wired
- `figma-generate-library` — external-service; required tools: adapter not wired
- `figma-implement-design` — external-service; required tools: adapter not wired
- `figma-use` — external-service; required tools: adapter not wired

## hyperframes (1)

- `hyperframes` — runtime-toolchain; required tools: adapter not wired

## image (4)

- `imagegen` — external-service; required tools: adapter not wired
- `imagen` — external-service; required tools: adapter not wired
- `pixelbin-media` — external-service; required tools: adapter not wired
- `replicate` — external-service; required tools: adapter not wired

## venice (5)

- `venice-audio-music` — external-service; required tools: adapter not wired
- `venice-audio-speech` — external-service; required tools: adapter not wired
- `venice-image-edit` — external-service; required tools: adapter not wired
- `venice-image-generate` — external-service; required tools: adapter not wired
- `venice-video` — external-service; required tools: adapter not wired

## video (7)

- `motion-frames` — runtime-toolchain; required tools: adapter not wired
- `remotion` — runtime-toolchain; required tools: adapter not wired
- `sora` — runtime-toolchain; required tools: adapter not wired
- `video-downloader` — runtime-toolchain; required tools: adapter not wired
- `video-hyperframes` — runtime-toolchain; required tools: adapter not wired
- `video-shortform` — runtime-toolchain; required tools: adapter not wired
- `youtube-clipper` — runtime-toolchain; required tools: adapter not wired
