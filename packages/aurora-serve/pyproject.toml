[project]
name = "aurora-serve"
version = "0.1.0"
description = "Aurora serve layer"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "aurora-core",
    "aurora-ext",
    "python-multipart>=0.0.26",
    "pandas>=2.0",
    "beautifulsoup4>=4.12",
    "openpyxl>=3.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aurora_serve"]
